# Agent Instructions

## Code Style
- Use semantic HTML5.
- Indentation: 2 spaces.
- Include `alt` text for every image. If the image is generated, include the prompt used to generate it in the `alt` text.

## Programmatic Checks
- Run `npx htmlhint` on all HTML files before committing.
